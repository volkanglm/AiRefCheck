/** AiRefCheck - postcss.config.mjs
 * PostCSS configuration with Tailwind CSS and Autoprefixer plugins.
 */

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
